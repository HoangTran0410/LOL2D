{
  "name": "@moba2d/content-riot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "assets:generate": "node scripts/generate-assets.mjs",
    "assets:check": "node scripts/generate-assets.mjs --check",
    "catalog:generate": "moba2d-generate-spell-catalog --tree=riot --root=.",
    "catalog:check": "moba2d-generate-spell-catalog --check --tree=riot --root=.",
    "typecheck": "tsc -p ../../tsconfig.pack-riot.json",
    "check-seams": "moba2d-check-seams ./spells",
    "check-seams:monsters": "moba2d-check-seams ./monsters"
  },
  "devDependencies": {
    "@moba2d/core": "*"
  }
}
